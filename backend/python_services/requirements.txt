# BERT Extractive Summarizer (existing)
bert-extractive-summarizer==0.10.1

# Core NLP Libraries
spacy==3.7.2
textblob==0.17.1

# Language Detection (Windows-compatible alternative to Polyglot)
langdetect==1.0.9

# Toxicity and Bias Detection
detoxify==0.5.2

# Transformer Models
transformers==4.36.2
torch==2.1.2
sentencepiece==0.1.99

# Explainability
shap==0.44.0

# Topic Modeling and Embeddings
gensim==4.3.2
# BERTopic with Windows-compatible dependencies
# Note: Install BERTopic separately after other packages if advanced topic modeling needed
# For Windows: pip install bertopic --no-deps, then manually install compatible versions
sentence-transformers==2.2.2
# umap-learn and hdbscan have compilation issues on Windows
# Using lightweight alternatives that work cross-platform

# Utilities
numpy==1.24.3
scikit-learn==1.3.2
pandas==2.1.4

# API Service
flask==3.0.0
flask-cors==4.0.0
