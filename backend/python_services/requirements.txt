# BERT Extractive Summarizer (existing)
bert-extractive-summarizer==0.10.1

# Core NLP Libraries
spacy==3.7.2
textblob==0.17.1

# Language Detection (Windows-compatible alternative to Polyglot)
langdetect==1.0.9

# Toxicity and Bias Detection
detoxify==0.5.2

# Transformer Models (updated to resolve dependency conflicts)
transformers>=4.41.0,<5.0.0
torch>=2.3.0,<3.0.0
sentencepiece==0.1.99

# Explainability
shap==0.44.0
lime==0.2.0.1

# Fairness
fairlearn==0.10.0

# Topic Modeling (Gensim LDA - Windows-compatible, no compilation required)
gensim==4.3.2

# ========================================
# EDA AND VISUALIZATION (OPTIONAL - Windows Long Path Issue)
# ========================================
# 
# NOTE FOR WINDOWS USERS:
# lux-api and sweetviz require Jupyter dependencies with very long file paths
# that may exceed Windows' 260-character path limit.
#
# SOLUTION 1 (Recommended): Enable Windows Long Path Support
# Run PowerShell as Administrator and execute:
#   New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
#     -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
# Then restart your computer.
#
# SOLUTION 2: Skip these packages (they are optional)
# Comment out the lines below if you cannot enable long paths.
# The system will work without EDA visualization features.
#
# lux-api==0.5.0
# sweetviz==2.3.1
#
# After enabling long paths OR if on Linux/macOS, uncomment to install:
lux-api==0.5.0
sweetviz==2.3.1

# Utilities (updated to resolve dependency conflicts)
numpy>=1.25.0,<2.0.0
scikit-learn>=1.3.2
pandas>=2.1.4

# API Service
flask==3.0.0
flask-cors==4.0.0

# ========================================
# OPTIONAL COMPONENTS (Not Required for Core Functionality)
# ========================================
# 
# Advanced Topic Modeling with BERTopic (Linux/macOS or Windows with C++ Build Tools):
# Uncomment and install only if you have Microsoft C++ Build Tools installed on Windows
# 
# sentence-transformers==2.2.2
# hdbscan==0.8.33
# umap-learn==0.5.5
# bertopic==0.16.0
#
# Installation on Windows (advanced users only):
# 1. Install Microsoft C++ Build Tools from https://visualstudio.microsoft.com/downloads/
# 2. pip install hdbscan umap-learn
# 3. pip install sentence-transformers bertopic
#
# Note: Gensim LDA provides excellent topic modeling without these dependencies
